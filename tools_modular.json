[
  {
    "name": "question_answering",
    "module": "chatbot",
    "description": "Answer questions by performing internet research and logical reasoning. Remembers context for follow-up questions.",
    "args_schema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "The natural language question to answer." }
      },
      "required": ["query"]
    },
    "output_format": "console"
  },
  {
    "name": "custom_api",
    "module": "tools.custom_api",
    "description": "Execute an internal project function exposed over HTTP.",
    "args_schema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "The natural language prompt or query for the tool." }
      },
      "required": ["query"]
    },
    "output_format": "console"
  },
  {
    "name": "scan_codebase",
    "module": "tools.development.codebase_tools",
    "description": "Scan and analyze codebase for patterns, conventions, folder structures, and naming patterns.",
    "args_schema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "Query about codebase patterns, folder structure, or naming conventions." }
      },
      "required": ["query"]
    },
    "output_format": "console"
  },
  {
    "name": "check_git_conventions",
    "module": "tools.development.git_tools",
    "description": "Check git commit history for naming conventions and development practices.",
    "args_schema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "Query about git conventions or commit patterns." }
      },
      "required": ["query"]
    },
    "output_format": "console"
  },
  {
    "name": "analyze_jira_history",
    "module": "tools.development.jira_tools",
    "description": "Analyze JIRA ticket history for development patterns and project insights.",
    "args_schema": {
      "type": "object",
      "properties": {
        "query": { "type": "string", "description": "Query about JIRA patterns or ticket analysis." }
      },
      "required": ["query"]
    },
    "output_format": "console"
  }
]
